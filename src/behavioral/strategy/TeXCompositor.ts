import Compositor from "./Compositor";

export default class TeXCompositor extends Compositor {
  compose(natural: any[], stretch: any[], shrink: any[], componentCount: number, lineWidth: number, breaks: number[]): number {
    //...
    return 0
  }
}